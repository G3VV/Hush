# Local configuration — may hold API tokens. Never commit it.
# `.env.example` is the committed template.
.env
.env.*
!.env.example

# Collected data — regenerated by running the collector
data/
*.db
*.db-wal
*.db-shm

__pycache__/
*.py[cod]

node_modules/
.DS_Store

# Vendored Leaflet — fetch with scripts/vendor-leaflet.sh
web/vendor/
